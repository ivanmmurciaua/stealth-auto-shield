name: auto-stealth-railgun-deposit
services:
  app:
    build: .
    env_file: .env
    volumes:
      - railgun-db:/app/.railgun-db
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  railgun-db:
